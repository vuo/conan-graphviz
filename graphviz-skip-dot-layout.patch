--- lib/dotgen/dotinit.c.old	2016-05-09 15:32:48.000000000 -0400
+++ lib/dotgen/dotinit.c	2016-05-09 15:33:17.000000000 -0400
@@ -506,8 +506,8 @@
 
 void dot_layout(Agraph_t * g)
 {
-    if (agnnodes(g)) doDot (g);
-    dotneato_postprocess(g);
+    // if (agnnodes(g)) doDot (g);
+    // dotneato_postprocess(g);
 }
 
 Agraph_t * dot_root (void* p)
